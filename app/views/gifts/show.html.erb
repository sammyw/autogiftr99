<p id="notice"><%= notice %></p>

<div class="row">
	<div class="col-md-3">
		<%= image_tag(@gift.image_url(:normal), class: 'pull-left img-thumbnail') %>
	</div>
	<div class="col-md-9">

		<h1><%= @gift.name %></h1>
		<p><%= @gift.description %></p>

		<p><%= number_to_currency(@gift.price) %></p>
	</div>
</div>